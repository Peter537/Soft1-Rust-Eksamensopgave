pub struct Circuit {
    pub id: i32,
    pub name: String,
    pub country_id: i32,
    pub country_name: String,
    pub city: String,
    pub length_km: f32,
    pub lap_amount: i32,
    pub image_path: String,
}
